{
    "name": "apt",
    "id": "apt",
    "version": "1.0.0",
    "description": "Installs APT packages",
    "options": {
	    "packages": {
			  "type": "string",
        "default": "make",
			  "description": "Packages to install"
		  }
    }
}
