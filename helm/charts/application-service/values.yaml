# === Ingress & Service === #
ingress: false
host: ""
healthCheckPath: "/"

# === Deployment === #
replicas: 1
port: 80
env: []
resources:
  limits:
    cpu: 50m
    ephemeral-storage: 1Gi
    memory: 64Mi
  requests:
    cpu: 50m
    ephemeral-storage: 1Gi
    memory: 64Mi

# === Secrets === #
secrets: {}

# === MySQL === #
# The MySQL deployment is only for testing purposes
mysql: false

